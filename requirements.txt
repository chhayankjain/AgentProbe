# Core dependencies — install with: pip install -e .
# This file is for convenience (e.g. CI, Docker); pyproject.toml is the source of truth.

# Agent frameworks
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0

# OpenTelemetry
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-exporter-otlp-proto-grpc>=1.27.0
opentelemetry-exporter-prometheus>=0.48b0

# Metrics & dashboards
prometheus-client>=0.21.0

# Utilities
httpx>=0.27.0
pydantic>=2.0.0
rich>=13.0.0
typer>=0.12.0
pandas>=2.0.0
matplotlib>=3.8.0
numpy>=1.26.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0

# Optional — install based on LLM backend:
# Ollama (local):  pip install langchain-ollama
# OpenAI:          pip install langchain-openai
# Anthropic:       pip install langchain-anthropic
# AutoGen:         pip install pyautogen
